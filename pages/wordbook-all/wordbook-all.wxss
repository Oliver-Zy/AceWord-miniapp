.container {
  width: 100%;
  height: 100vh;
  background-image: linear-gradient(180deg, #EDE9FE -10%, #F9FAFB 30%);
  /* 与项目整体保持一致的紫色系渐变背景 */
}

/* 搜索框 */
.search-container-fixed {
  margin: 0 16px 16px 16px;
  padding: 0;
}

.search-box {
  height: 36px;
  background: #FFFFFF;
  border: 0.5px solid #E9ECF2;
  border-radius: 18px;
  display: flex;
  align-items: center;
  padding: 0 12px;
}

.search-icon {
  width: 20px;
  height: 20px;
  margin-right: 8px;
}

.search-placeholder {
  font-size: 14px;
  color: #86909C;
  font-weight: 400;
}

/* 搜索结果 */
.search-results-container {
  margin: 0 16px 16px 16px;
}

.search-results-group {
  background: #FFFFFF;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  border: 1px solid #F1F3F4;
  overflow: hidden;
}

/* 分类卡片 */
.category-card {
  margin: 0 16px;
  background: #FFFFFF;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
  border: 1px solid #F1F3F4;
  overflow: hidden;
}

/* 折叠面板自定义样式 */
.collapse-item {
  background: transparent !important;
  --collapse-item-content-padding: 0;
}

/* 分类数量 */
.category-count {
  font-size: 13px;
  color: #86909C;
  font-weight: 500;
}

/* 词书组 */
.wordbook-group {
  background: transparent !important;
  margin: 0 !important;
}

/* 词书单元格 */
.wordbook-cell {
  background: transparent !important;
  align-items: center !important;
}

/* 调整Cell内容区域宽度和对齐 */
.wordbook-cell .van-cell__title {
  flex: 2 !important;
  min-width: 0 !important;
}

.wordbook-cell .van-cell__value {
  flex: 0 0 auto !important;
  text-align: right !important;
  align-self: center !important;
  margin-left: 12px !important;
}

.wordbook-cell .van-cell__right-icon {
  align-self: center !important;
  margin-left: 8px !important;
}

/* 当前词书样式 */
.current-wordbook {
  background: linear-gradient(135deg, #EDE9FE 0%, #DDD6FE 100%) !important;
  border-left: 3px solid #6366F1 !important;
}

/* 词书标签 */
.book-tags {
  display: flex;
  gap: 4px;
  align-items: center;
  flex-shrink: 0;
  max-width: 120px;
  align-self: center;
}

/* 空状态 */
.empty-state {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 60px 20px;
}

/* 子分类头部 */
.subcategory-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px 8px 16px;
  background-color: #f8f9fa;
  border-bottom: 1px solid #ebedf0;
}

.subcategory-title {
  font-size: 14px;
  font-weight: 500;
  color: #323233;
}

.subcategory-count {
  font-size: 12px;
  color: #969799;
}

/* 深色模式适配 */
@media (prefers-color-scheme: dark) {
  .container {
    background-image: linear-gradient(180deg, #1E1B4B -10%, #1F2937 30%);
  }

  .search-box {
    background: #282A3D;
    border: 0.5px solid #3A3D50;
  }

  .search-placeholder {
    color: #9598A9;
  }

  .search-results-group {
    background: #282A3D;
    border: 1px solid #3A3D50;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  }

  .category-card {
    background: #282A3D;
    border: 1px solid #3A3D50;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  }
}